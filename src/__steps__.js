/**
 * react router setup
 * -------------------------
 * 1. npm create vite@latest name-of-your-project -- --template react
 * 2. cd <your new project directory>
 * 3. npm install react-router-dom localforage match-sorter sort-by
 * 4. vs code open
 * 5. npm run dev
*/

/**
 * tailwind & daisyUI setup
 * ----------------------------
 * 1. npm install -D tailwindcss postcss autoprefixer
 * 2. npx tailwindcss init -p
 * 3. @type {import('tailwindcss').Config} 
 * 4. add index.css
 * 5. npm run dev
 * 
 * 1. npm i daisyui
 * 2. Then add daisyUI to your tailwind.config.js files:
*/

/**
 * firebase setup
 * --------------------
 * 1. get started
 * 2. add project
 * 3. npm install firebase
 * 4. Then, initialize Firebase and begin using the SDKs for the products you'd like to use.
 * 5. export default app
 * 6. Build > Authentication > Web > Get Started
*/

/**
 * context api
 * -------------
 * 1. create context (export)
 * 2. set provider
 * 3. set context value
 * 4. to access the value: useContext hook
 * 
 * share
 * ------------
 * 1. main.jsx set AuthProviders
 * 2. inside provider access children props and then use
*/